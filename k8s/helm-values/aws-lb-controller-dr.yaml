# helm repo add eks https://aws.github.io/eks-charts
# helm upgrade --install aws-load-balancer-controller eks/aws-load-balancer-controller \
#   -n kube-system -f aws-lb-controller-dr.yaml
clusterName: shopcloud-dr
region: eu-west-1
vpcId: ""        # fill in with Terraform DR output vpc_id
image:
  tag: v2.8.2
serviceAccount:
  create: true
  name: aws-load-balancer-controller
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::${AWS_ACCOUNT_ID}:role/shopcloud-dr-irsa-aws-lb-controller
replicaCount: 2
resources:
  requests: { cpu: 50m,  memory: 96Mi }
  limits:   { cpu: 200m, memory: 256Mi }
enableShield: false
enableWaf: false
enableWafv2: true
