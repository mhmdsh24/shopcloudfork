apiVersion: v1
kind: ServiceAccount
metadata:
  name: checkout
  namespace: shopcloud
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::781863099565:role/shopcloud-primary-irsa-checkout
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: checkout
  namespace: shopcloud
  labels: { app: checkout, tier: api }
spec:
  replicas: 2
  revisionHistoryLimit: 3
  strategy: { type: RollingUpdate, rollingUpdate: { maxSurge: 1, maxUnavailable: 0 } }
  selector: { matchLabels: { app: checkout } }
  template:
    metadata:
      labels: { app: checkout, tier: api }
    spec:
      serviceAccountName: checkout
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: DoNotSchedule
          labelSelector: { matchLabels: { app: checkout } }
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: checkout
          image: 781863099565.dkr.ecr.us-east-1.amazonaws.com/shopcloud/checkout:latest
          ports: [{ name: http, containerPort: 8080 }]
          env:
            - { name: LOG_LEVEL, value: info }
            - { name: AWS_REGION, value: us-east-1 }
            - { name: EVENT_BUS_NAME, value: shopcloud-primary-events }
          envFrom:
            - secretRef: { name: checkout-secrets, optional: true }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 500m, memory: 256Mi }
          livenessProbe:
            httpGet: { path: /healthz, port: http }
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /ready, port: http }
            initialDelaySeconds: 5
            periodSeconds: 5
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities: { drop: ["ALL"] }
          volumeMounts:
            - { name: tmp, mountPath: /tmp }
      volumes:
        - { name: tmp, emptyDir: {} }
---
apiVersion: v1
kind: Service
metadata: { name: checkout, namespace: shopcloud, labels: { app: checkout } }
spec:
  type: ClusterIP
  selector: { app: checkout }
  ports: [{ name: http, port: 8080, targetPort: http }]
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: { name: checkout, namespace: shopcloud }
spec:
  maxUnavailable: 1
  selector: { matchLabels: { app: checkout } }
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: checkout, namespace: shopcloud }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: checkout }
  minReplicas: 2
  maxReplicas: 4
  metrics:
    - type: Resource
      resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } }
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: { name: checkout, namespace: shopcloud }
spec:
  podSelector: { matchLabels: { app: checkout } }
  policyTypes: ["Ingress", "Egress"]
  ingress:
    - from: [{ ipBlock: { cidr: 10.0.0.0/16 } }]
      ports: [{ port: 8080, protocol: TCP }]
  egress:
    - to: [{ namespaceSelector: {} }]
      ports: [{ port: 53, protocol: UDP }, { port: 53, protocol: TCP }]
    # talk to cart + auth
    - to:
        - podSelector: { matchLabels: { app: cart } }
        - podSelector: { matchLabels: { app: auth } }
      ports: [{ port: 8080, protocol: TCP }]
    # RDS + Redis
    - to: [{ ipBlock: { cidr: 10.0.20.0/23 } }]
      ports:
        - { port: 5432, protocol: TCP }
        - { port: 6379, protocol: TCP }
    # AWS APIs (EventBridge, Secrets Manager)
    - to:
        - ipBlock: { cidr: 0.0.0.0/0, except: ["169.254.169.254/32"] }
      ports: [{ port: 443, protocol: TCP }]
